layui.define(["TableColTemplet","jquery","yunj"],function(e){let t=layui.TableColTemplet;let a=window;let i=document;let l=layui.jquery;class u extends t{constructor(e){super(e)}layout(){let e=this;return`{{# 
                        let value = d.${e.key};
                        let datetime = '';
                        if(yunj.isDatetime(value)){
                            value = yunj.datetimeToTimestamp(value);
                        }
                        if(yunj.isTimestamp(value)){
                            datetime = yunj.timestampFormat(value);
                        }
                        if(datetime) { 
                    }}
                     <span class="layui-badge layui-bg-gray table-row-datetime" title="{{ datetime }}">{{ datetime }}</span>
                    {{# } }}`}defineExtraEventBind(){let e=this;if(yunj.isUndefined(a.TABLE_ROW_DATETIME_CLICK_COPY_EVENT_BIND)){a.TABLE_ROW_DATETIME_CLICK_COPY_EVENT_BIND=true;l(i).on("click",".table-row-datetime",function(e){yunj.copy(l(this).text());e.stopPropagation()})}}}e("TableColDatetime",u)});
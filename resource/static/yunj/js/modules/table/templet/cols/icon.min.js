layui.define(["TableColTemplet","jquery","yunj"],function(l){let e=layui.TableColTemplet;let t=window;let i=document;let n=layui.jquery;class a extends e{constructor(l){super(l)}layout(){let l=this;return`{{# 
                        let val = d.${l.key};
                        if(!yunj.isString(val)) {
                            val = "";
                        }
                        val = yunj.iconClass(val);
                     }}
                    {{#  if(d.is_export || !val){  }}
                        {{val}}
                    {{#  }else{  }}
                        <div class="table-row-icon" title="{{val}}">
                            <i class="{{val}}"></i>
                        </div>
                    {{#  }  }}`}defineExtraEventBind(){let l=this;if(yunj.isUndefined(t.TABLE_ROW_ICON_CLICK_VIEW_EVENT_BIND)){t.TABLE_ROW_ICON_CLICK_VIEW_EVENT_BIND=true;n(i).on("click",".table-row-icon",function(l){let e=n(this).attr("title");yunj.copy(e);l.stopPropagation()})}}}l("TableColIcon",a)});